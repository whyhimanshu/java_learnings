package Questions.q77.geometry;

public class Rectangle {
    public int len;
    public int breadth;

    public Rectangle(int breadth, int len) {
        this.breadth = breadth;
        this.len = len;
    }
}
